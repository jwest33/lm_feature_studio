* Fix steering vector scalar input to allow user to specify many decimal points
* Add mechanism to fine-tune model based on applied steering vectors
* Add frontend mechanism for user to select base model, and SAE width/l0 etc
